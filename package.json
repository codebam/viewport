{
  "name": "viewport-shell",
  "version": "0.1.5",
  "private": true,
  "description": "Build-time dependencies of the reference shell in data/shell.",
  "license": "MIT",
  "scripts": {
    "vendor": "scripts/vendor-shell-deps.sh",
    "test": "node tests/shell.test.js data/shell tiling && node tests/shell.test.js data/shell scrolling && node tests/shell.test.js data/shell matrix && node tests/shell.test.js data/shell canvas"
  },
  "dependencies": {
    "gsap": "^3.15.0"
  }
}

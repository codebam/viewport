build*/
result
result-*
.cache/
compile_commands.json
*.o
*.so
__pycache__/
/target
packaging/out/
packaging/arch/out/
*.pkg.tar.*
bench-results/

# The cef shell is its own cargo workspace, so its build directory is not
# covered by the rule for the one at the root.
crates/viewport-shell-cef/target/

.wrangler/

# npm resolves the shell's dependencies here; what the shell actually loads is
# the built copy under data/shell/vendor, which is committed. See
# scripts/vendor-shell-deps.sh.
node_modules/

# Linked worktrees created by the agent, e.g. .worktrees/fix-mod4-shift-q.
.worktrees/

# zvec-grep index
.zvec-grep/

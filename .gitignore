build*/
result
result-*
.cache/
compile_commands.json
*.o
*.so
__pycache__/
/target
packaging/arch/out/
*.pkg.tar.*
bench-results/

# The cef shell is its own cargo workspace, so its build directory is not
# covered by the rule for the one at the root.
crates/viewport-shell-cef/target/
